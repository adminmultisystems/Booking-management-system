package com.hotelsystems.ai.bookingmanagement.supplier.hotelbeds;

import org.junit.jupiter.api.Disabled;

/**
 * Tests for HotelbedsClient using MockWebServer.
 * 
 * NOTE: This test class is disabled for Phase-1 as it tests real supplier integration
 * (HotelbedsClient, HotelbedsAuthFilter, etc.) which is not part of Phase-1 scope.
 * Phase-1 only includes stub supplier adapters.
 * 
 * These tests will be enabled when real supplier integration is implemented in later phases.
 * The original test implementations are preserved in git history.
 */
@Disabled("Disabled for Phase-1 - tests real supplier integration, not stub adapters")
class HotelbedsClientTest {
    // Test methods removed for Phase-1 - will be restored when HotelbedsClient is implemented
}
