spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: 
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
  h2:
    console:
      enabled: false

flyway:
  enabled: true
  locations: classpath:db/migration

supplier:
  hotelbeds:
    base-url: https://api.test.hotelbeds.com
    api-key: test-key
  travellanda:
    base-url: https://api.test.travellanda.com
    api-key: test-key

