# H2 Database Configuration for Development
# Use this profile if you don't want to install PostgreSQL
# Run with: java -jar app.jar --spring.profiles.active=h2

spring:
  application:
    name: Hotelsystems.ai

  # H2 Database Configuration (In-Memory)
  datasource:
    url: jdbc:h2:mem:hotelsystems;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""

  # H2 Console (Optional - for viewing data in browser)
  # Access at: http://localhost:8080/h2-console
  # JDBC URL: jdbc:h2:mem:hotelsystems
  h2:
    console:
      enabled: true
      path: /h2-console

  # JPA/Hibernate Configuration for H2
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
        format_sql: true

# Pricing Intelligence Configuration (Optional for local/testing)
# Dummy pricing is used by default - no external API calls are made
# pricing:
#   intelligence:
#     base-url: https://api.pricing-intelligence.example.com
#     api-key: your-api-key-here
#     timeout-ms: 5000

